networks:
  1panel-network:
    external: true

services:
  rainbow-dnsmgr:
    image: netcccyun/dnsmgr@sha256:4bc57a2832b0d2687dc6a8bd3c4d3a85700faec4f3d56433545380b5f0b861f4
    container_name: ${CONTAINER_NAME}
    restart: always
    stdin_open: true
    tty: true
    deploy:
      resources:
        limits:
          cpus: ${CPUS}
          memory: ${MEMORY_LIMIT}
    environment:
      TZ: Asia/Shanghai
      PANEL_DB_HOST: ${PANEL_DB_HOST}
      PANEL_DB_PORT: ${PANEL_DB_PORT}
      PANEL_DB_NAME: ${PANEL_DB_NAME}
      PANEL_DB_USER: ${PANEL_DB_USER}
      PANEL_DB_USER_PASSWORD: ${PANEL_DB_USER_PASSWORD}
      PANEL_DB_PREFIX: ${PANEL_DB_PREFIX}
    labels:
      createdBy: Apps
    networks:
      - 1panel-network
    ports:
      - ${HOST_IP}:${PANEL_APP_PORT_HTTP}:80
    volumes:
      - ./data/rainbow-dnsmgr:/app/www
